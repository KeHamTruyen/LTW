<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # If already in public/, don't redirect again
    RewriteCond %{REQUEST_URI} !^/public/
    
    # Redirect to public/
    RewriteRule ^$ public/ [L]
    RewriteRule (.*) public/$1 [L]
</IfModule>